{
  "name": "cto-dashboard",
  "version": "1.0.0",
  "description": "A dashboard for CTO to monitor project metrics",
  "scripts": {
    "build:css": "tailwindcss -i ./src/css/input.css -o ./app/static/css/output.css",
    "watch:css": "tailwindcss -i ./src/css/input.css -o ./app/static/css/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "daisyui": "^3.9.4",
    "tailwindcss": "^3.3.5"
  }
}